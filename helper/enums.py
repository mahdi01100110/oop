from enum import Enum
class ExitCommand(Enum):
    """ this enum class define four commands to use theme to get out the program """
    exit = "exit"
    ex = "ex"
    q = "q"
    quit = "quit"
    esc = "esc"
    